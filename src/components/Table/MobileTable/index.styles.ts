import styled from '@emotion/styled';
import { TableCell } from '@mui/material';

export const MobileTableCell = styled(TableCell)`
  padding-left: 0;
  padding-right: 0;
  text-align: right;
`;

export const TblBodyText = styled.p`
  margin: 0;
  font-size: 15px;
`;
